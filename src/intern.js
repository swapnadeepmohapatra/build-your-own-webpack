function add(x, y) {
  return x + y;
}

function subtract(x, y) {
  return x - y;
}

function multiply(x, y) {
  return x * y;
}

function divide(x, y) {
  return x / y;
}

const greet = "Hello Swapnadeep";

console.log(`20 + 10 = ${add(20, 10)}`);
console.log(`20 * 10 = ${multiply(20, 10)}`);
console.log(`20 - 10 = ${subtract(20, 10)}`);
console.log(`20 / 10 = ${divide(20, 10)}`);

console.log(greet);
